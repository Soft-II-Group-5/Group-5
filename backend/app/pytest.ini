[pytest]
testpaths = tests
python_files = test_*.py
markers =
  unit: fast isolated tests
  validation: high-level component/use-case checks
  integration: multiple components together
  system: tests in a different environment
addopts = -q
